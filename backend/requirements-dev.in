-r requirements.in

# Testes
pytest ~= 8.3
pytest-asyncio ~= 0.24
factory-boy ~= 3.3
httpx ~= 0.27

# Análise estática (saíram de requirements.in no saneamento pré-Marco 8).
# Lint e type-check são etapas de CI/dev, não precisam entrar na imagem de prod.
# Pins compatible-release: preservam o major.minor das versões atualmente
# resolvidas (ruff 0.15.x, mypy 1.20.x), permitindo só upgrades de patch.
ruff ~= 0.15
mypy ~= 1.20
