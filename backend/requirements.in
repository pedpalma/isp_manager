# Dependências de runtime
# Após editar, rode: make deps-compile

# Web framework
fastapi[standard] ~= 0.136.0
uvicorn[standard] ~= 0.30


# Validação e configuração
pydantic ~= 2.13
pydantic-settings ~= 2.14


# Banco de dados
sqlalchemy[asyncio] ~= 2.0.50
asyncpg ~= 0.30
# psycopg (v3) é usado pelo Alembic (sync). Marco 4.
psycopg[binary] ~= 3.2
alembic ~= 1.13


# Segurança
pyjwt ~= 2.9
argon2-cffi ~= 23.1


# Observabilidade
structlog ~= 24.4


# Dev tooling
pip-tools ~= 7.4

# Workers
celery~=5.4
redis~=5.0
